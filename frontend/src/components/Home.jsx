// import { useSelector } from "react-redux";

const Home = () => {
  // const auth = useSelector((state) => state.auth);
  // console.log("auth", auth);
  return <h2>Home</h2>;
};

export default Home;
